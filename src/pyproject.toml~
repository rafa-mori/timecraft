[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.setuptools.packages.find]
where = ["src"]
include = ["timecraft*", "dbconnect*", "classify*", "regression*"]
exclude = ["tests*"]

[project]
name = "timecraft"
version = "0.1.0"
requires-python = ">=3.7"
authors = [
  { name = "Rafael Mori", email = "faelmori@gmail.com" }
]
maintainers = [
  { name = "Rafael Mori", email = "faelmori@gmail.com" }
]
description = "Biblioteca para análise de séries temporais e integração com bancos de dados"
readme = "README.md"
license = { text = "MIT" }
keywords = ["time series", "database", "automation", "forecasting", "data analysis", "regression"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Operating System :: OS Independent"
]

dependencies = [
  "numpy>=1.21.0",
  "pandas>=1.3.0",
  "matplotlib>=3.4.0",
  "sqlalchemy",
  "pyodbc",
  "scikit-learn",
  "prophet",
  "requests",
  "jinja2",
  "mysql-connector-python",
  "pymongo",
  "xarray",
  "seaborn",
  "plotly",
  "kaleido"
]

[project.optional-dependencies]
oracle = ["cx_Oracle"]
mssql = ["pymssql"]
ml = ["scikit-learn", "prophet"]
viz = ["seaborn", "plotly", "kaleido"]

[project.urls]
Homepage = "https://github.com/faelmori/timecraft"
Documentation = "https://github.com/faelmori/timecraft/docs"
Repository = "https://github.com/faelmori/timecraft"
"Bug Tracker" = "https://github.com/faelmori/timecraft/issues"

[project.scripts]
timecraft-cli = "timecraft.cli:main"

